#include "bits/stdc++.h"

using namespace std;


void extend(int w) {
	int p = ++tot, x = last, r, q;
}


int main() {
	string str;
	cin >> str;
	for (int i = 0; i < str.size(); ++i) {
		extend(str[i] - 'a' + 1);
	}
}